<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ✅ Spring Boot 기본 콘솔 스타일 유지 -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <!-- ✅ AdController 로그 파일 저장 (20분 단위처럼 보이게) -->
    <appender name="ADCONTROLLER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>../logs/service/ad-service/adController.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- ✅ 분 단위 롤링 → 20분 단위처럼 구분 가능 -->
            <fileNamePattern>../logs/service/ad-service/adController.%d{yyyy-MM-dd_HH-mm}.log</fileNamePattern>
            <maxHistory>90</maxHistory>
        </rollingPolicy>
        <encoder>
            <!-- 운영용: 시간, 스레드, 레벨, 클래스명 포함 -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ✅ 루트 로거: 콘솔 출력 유지 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- ✅ AdController 전용 로거 -->
    <logger level="INFO"
      name="edu.goorm.ad_service.controller.AdController"
      additivity="false">
        <appender-ref ref="ADCONTROLLER"/>
    </logger>

</configuration>
